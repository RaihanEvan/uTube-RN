import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
//import Header from '../components/Header'


const LoginScreen = ()=>{
   return(
       <View style={{flex:1}}>
           <Text style={{marginTop:25}}>This is the login / Authentication Screen</Text>
       </View>
   )
}

export default LoginScreen